# Cursor Integration (MVP)

## Primary strategy
- Agent uses AgentGuard MCP tools for actions:
  - terminal commands
  - applying file patches
  - git actions

## Extension compatibility
- Cursor supports VS Code extensions; the extension is the UX plane.

## Post-MVP
- Add hooks integration as an observer to detect off-path actions and enrich audit.
